---
type: session
session: 2026-08-11
status: resolved
---
# Handoff — 2026-08-11 (threshold-0.35-only verified + ported to production; pivot rule abandoned; session wrapped)

## Where the work is
Prototype fix implemented + tested (14/14) and rerun on all 4 maps via 4 parallel sub-agents. Feral (the target) is fully fixed per the user's walk; YOASOBI (fully user-verified control) breaks at 106 rows → the old kill-criteria fired. Key separation finding: **the pivot rule causes ~all the damage; R_THRESHOLD 0.35 causes ~none and is itself the Feral Class B fix**. The user redesigned the kill-criteria to be clue-based ([[experiment-protocol]]) and will decide the pivot rule under it next session. Production port still gated.

## Findings (evidence: `Prototyping/rerun_{map}.json` ×4, agent reports)

| Map | Fixes | Net-new changes | Accepted cases flipped? |
|---|---|---|---|
| Feral (target) | 7/7 Class A + 6/6 Class B | 24 moves, all merges, zero fragmentation (18/24 NC-corroborated; 6 non-NC slider-region: 17.891, 72.735, 74.235, 75.735, 77.235, 108.922) | Walk sites all satisfied |
| YOASOBI (control) | 0 (already correct) | 106 rows diverge from walk: 39 merges (6 pickup-joins spanning 2–5 combo sections), 12 boundary shifts | 106 rows flip → kill fired |
| Signal | 02:08.539 byte-identical | 21 pivot moves (all speed-up), 5 fragments, 24 merges; 413→397 | No full walk |
| Heart Pie | 638ms stack byte-identical | 19 singletons vs 76 merges; NC corroborates 4/4 pivots; 608→566 | Walk = 1 site, untouched |

- **Threshold vs pivot rule (user's question):** on YOASOBI the pivot rule ≈ 100% of the damage (a threshold drop can only ADD boundaries → splits; agent found zero fragmentations; every changed row traces to a k+1→k move). On Signal the agent verified the threshold alone created/removed no sites. **The threshold drop is the Feral Class B fix itself** (R = |log2(125/93.75)| = 0.415 ∈ (0.35, 0.5); slow-down boundaries at k+1, nc=False) — user's suspicion confirmed. Candidate for next session: **threshold-0.35-only** = Feral stream fix with zero YOASOBI damage; pivot rule decided separately.
- **NC-gate hypothesis: dead.** All 6 YOASOBI join pivots carry NC=1 — same signal as Feral's pivots, opposite walk outcome. Combo-boundary gate (reject moves creating mid_combo_breaks>0 patterns) kills 6/6 joins but is partial (≈4 dissolves survive). No clean separator found; the rule encodes Feral's mapper intent, YOASOBI's is opposite at the same signals.
- **Gap-rule re-verify: "9 stacks ALL gap-rule" REFUTED** — 0/9 gaps > 197.5ms (187.0/94.0ms); all 9 were R-discontinuity (1/4↔1/2, R=0.99) in both runs; stacks 6–9 now type-rule-held (rows unchanged). The 08-10 "90ms @ 375 BPM" was a BPM misreading; code always used 197.5ms. Also: Feral has 18 total 1/3→1/6 speed-up sites (7 in the walk + 11 same-family, all nc-on-pivot) + 2 new-fire 1/3→1/4 sites.

## Decisions (this session)
- **User: kill-criteria must be clue-based, not damage-based** ("stop after $1M spent" is useless; detect the clues first). [[experiment-protocol]] upgraded: pre-flight (premise coverage ≥90%, would-be damage on walked controls, exposure→canary-first) | canary (first-map check gates the fan-out; any accepted flip → stop) | final (classic counts as confirmation). Retrospective: today's run would have stopped after the YOASOBI canary.
- **User: delegation tiers for this session** — reading sources + mechanical wiki maintenance (lint, link renames, index lines) → sub-agents; drafting pages from spec excluded (re-evaluate); verdicts/decisions/wiki writing stay main. CLAUDE.md §7 classification test (user-approved): every task tagged `[delegate]`/`[main]` before tool use; "fan out" override. Evaluation: 4-map fan-out + 2 follow-ups all complete/structured; main context lean. Continues.
- **Infra:** wiki skills fixed — were nested at `.claude/skills/wiki/` (harness registers only `.claude/skills/<name>/`, so `/wiki-lint` etc. never loaded); flattened + `.gitignore` exceptions updated. Session directives → handoff §7.
- **Prototype fix shipped (test-only, production untouched):** `DISCONTINUITY_THRESHOLD` 0.35 (:130), `pivot_boundary_idx` helper in R loop (~:344) and T loop (~:326), header doc bullet; 3 pivot unit tests; 14/14 pass.

## Open decisions (2026-08-11 session — resolved)
1. **Pivot rule** → **ABANDONED** (user, 2026-08-11: "I give up optimizing on pivot rule"). R_THRESHOLD 0.35 stands — verified (canary PASS, user in-game cross-check clean). Class A boundary placement = known limitation. Doc: `abandoned/abandoned_pivot-rule.md` (repo root, not in wiki).
2. Keep/abandon [[experiment-protocol]] → **RESOLVED 2026-08-12:** protocol STOPPED (paused, not abandoned) — user switched to the research-agent role-split mode (main = research agent, builder sub-agent, critic) for the motor modelling discussion; see log.md.
3. Production port (R_THRESHOLD 0.35 at rhythm_segmentation.rs :30; pivot rule :152–165/:207–219) → **left open**.
4. Research-loop items #2–#5 (pre-build critic, batch ranking, builder split, golden maps) → **left open**. User note: wants the clue-based kill-criteria exercised when we return to the **motor adjustment plan** ([[sequence-motor]]).
5. Excel walk leftovers → user: **rerun YOASOBI + Feral only** with Excels for in-game cross-check; Signal 18 / Heart Pie 1 / YOASOBI pickup joins 511/360/1054 deferred; Feral 9 moot (stacks refuted as gap-rule).

## Session end — next session (resume token: `resume:<next-date>`; focus: motor adjustment plan)
- [x] Recap-only per CLAUDE.md §6 — delivered; user authorized execution (decisions above).
- [x] Rerun DONE + VERDICT (2026-08-11): threshold-0.35-only canary PASS (YOASOBI 549/549 row-identical); Feral 385 (+6, all 1/4↔1/3), Unstable 52→47; **user in-game cross-check: "no problem"** → threshold-0.35-only = VERIFIED + ACCEPTED. Excels: `Prototyping/{yoasobi,feral}_thr035_patterns_exp.xlsx`.
- [x] Pivot rule **abandoned** (user decision) + documented: `abandoned/abandoned_pivot-rule.md`; pulled from scope; [[rhythm-segmentation]] updated.
- [x] **Production port DONE + committed** (2026-08-11): `rhythm_segmentation.rs` = exp rules + R/T 0.35; `finger_control::patterns` (Path A) DELETED; finger_control + reading rewired; prototype pivot code removed (11/11 tests); all `cargo test` green; **canary PASS — production row-identical to YOASOBI 549 + Feral 385 on all 7 fields** (`canary_segmentation.rs` kept as regression harness). No Signal/Heart Pie runs (user: not needed).
- [x] Exp #1 verdict (2026-08-11, user in-game cross-check clean): **threshold-0.35-only = VERIFIED + ACCEPTED** (pre-flight ✓, canary PASS, walk no problems) → port candidate; **pivot rule = OPEN experiment** (canary-failed on YOASOBI, gated off; refinement/rejection pending).
- [x] Wiki lint verdict handled: sessions 08-08/09/10 marked `resolved` (08-11 stays open — live handoff); [[sequence-motor]] R=0.5 stale text left open (user decision).
- [ ] **Next session:** motor adjustment plan ([[sequence-motor]]) — user continues the clue-based kill-criteria evaluation there; research-loop #2–#5 open; production port regression harness = `canary_segmentation.rs`.

## Pending / next session (resume token: `resume:2026-08-11`)
- [ ] Recap-only per CLAUDE.md §6 until user authorizes execution.
- [ ] User decision #1 above → then (a) threshold-only candidate: prototype `DISCONTINUITY_THRESHOLD` back to... keep 0.35, pivot rule gated off — rerun per NEW clue-based criteria (canary YOASOBI first, static pre-flight checks); (b) or refine pivot rule with combo-boundary gate; (c) or reject → Feral errors documented as known limitations.
- [ ] Record exp #1 verdict under the new criteria when decided ([[experiment-protocol]] entry).

## Save state
Working tree, uncommitted (commit pending human approval per save ritual): prototype fix (`backend/src/bin/prototype_sequence_motor.rs`), `Prototyping/rerun_{map}.json` ×4, CLAUDE.md §7 classification test, `.claude/skills/` flatten (renames staged), `.gitignore` exceptions, wiki: [[experiment-protocol]] (clue-based criteria), log.md (2 entries), index.md, this handoff. Backend production untouched. Scratch: agent reports in task transcripts (not on disk beyond rerun JSONs).

## Sources
Agent rerun reports (4 maps + 2 YOASOBI follow-ups), `Prototyping/rerun_*.json` ×4, `Prototyping/*_patterns_exp.json` references, `D:\osu files\` raw maps, prototype_sequence_motor.rs (edited), [[experiment-protocol]] (criteria upgrade), log.md; predecessor: the pre-rerun version of this file (rewritten in place 2026-08-11).
