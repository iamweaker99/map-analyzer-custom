---
type: session
session: 2026-08-13
status: resolved
---
# Handoff — 2026-08-13 (OIAH diagnostic of the motor metrics: complete — ingested as [[motor-metrics-oiah]]; commit pending human review)

## Where the work is
Resumed from `resume:2026-08-12` (recap-only → user authorized). This session ran an **OIAH (Observation/Inference/Assumption/Hypothesis) diagnostic** of why the motor adjustment metrics (**MM / MPA / SC**) cannot reproduce the user's tier ranking of sections (purple > orange > aqua). **All phases complete.** Phase 4 (reasoning map appended to `Temp/oiah_motor_adjustment.md`, presented in chat) and Phase 5 (placement decided + wiki written) done this session; the wiki diff awaits **human review → commit** (save ritual). Resume token: `resume:2026-08-13`.

## Findings

- **OIAH methodology** (`Temp/OIAH definitions & pipeline.md`, "Experiment Reasoning Framework"): four strictly distinct categories; O is root, I from ≥1 O, H from I and/or A; **mandated many-to-many reasoning graph** with traceability to observations, no invented upstream evidence, competing hypotheses preserved, downgrade-not-upgrade; per-item fields (Obs: Evidence/Source; Inf: Confidence; Asp: Why it matters/Evidence status; Hyp: Prediction/Test/Falsifier). User's naming convention: Obs./Inf./Asp./Hyp.
- **Metrics identified (wiki + code):** MPA = mean absolute second difference of spacings (≥4 notes), MM = RMS spacing (≥2), SC = CV of spacings (≥3) — spacing-only per-pattern aggregates in `sequence_motor` (`backend/src/analysis/reading/sequence_motor.rs:72-112`, normalization `reading/mod.rs:12-14`), deliberately no single score.
- **Excel structure:** `Prototyping/yoasobi_collab_extra_motor_patterns (success criteria here).xlsx` — single sheet, 552 rows, 9 cols; tiers = cell fill only, no legend (purple theme#7 22 rows / orange theme#9 33 / aqua theme#8 30; rows 56–57 lighter orange = **accident, user-confirmed, 3 colors stand**); 51 of 85 tiered rows carry descriptor text; A552 defines "abruptly" = broken minor-section joins, cursor reset under same BPM snapping window — the workbook's only in-file difficulty definition.
- **Gap table + diagnosis (Phases 1–2):** the triple models spacing variability *inside* an atomic pattern; the workbook's defined mechanism (abrupt joins, A552) and geometry concepts (self-overlap, spirals, direction turns, stacked transitions) are unmeasured. Tier means order P>O>A for MPA/SC but MM is near-flat, and ranges overlap across all tiers (3-way overlap covers ~all of each range) → no threshold/weighting can rank rows. Coverage holes: 32/85 tiered rows (P7/O15/A10 — all 1-note Jumps/Sliders) have **no metric values** (excluded by ≥2-note rule), incl. purple r343/364/367/512 which carry the most explicit difficulty descriptors; short bursts force-zeroed (MPA needs ≥4 notes; purple r301, orange r139 score 0/0/0). Wiki's existing "no separation" record confirmed (r542 "the hardest" at MPA .358/MM .952; untagged reach 4.7).
- **OIAH generated (Phase 3)** → `Temp/oiah_motor_adjustment.md`: **13 Observations + 7 Inferences + 6 Assumptions + 7 Hypotheses**, each with doc-conformant fields + "Based on" links (many-to-many). Key items: Inf.3 (the defined mechanism lives at boundaries the metrics can't see), Inf.5 (coverage holes coincide with the strongest purple evidence), Inf.7 (difficulty ≈ spacing variability + cross-boundary motor-reset demand, two orthogonal components); Hyp.1 (trajectory descriptors — turn-sign sequence sᵢ ∈ {+,−,0}, shapes, self-overlap), Hyp.2 (join-boundary discontinuity, the purple discriminator, HH1 fork), Hyp.3 (covering the 32 excluded rows), Hyp.4 (locality-preserving MPA, max not mean), Hyp.5 (geometry channel strengthens the proxy — AA1 path), Hyp.6 (section aggregation preserves locality), Hyp.7 (**competing**: difficulty may not be measurable from trajectory geometry → criterion revision / cognition layer).
- **Correction recorded:** Phase 2's "abrupt appears in 9/14 purple descriptors" was wrong — recount from the tag table: abrupt 5/14, self-overlap 5/14, direction-turn 7/14 (10/14 invoke ≥1 of these); Obs.9 carries the corrected counts.
- **User confirmations this session:** sections = colored rows; a color tag may cover a run of consecutive rows executed in sequence ("similar to those 51 test sample notes") → **Obs.14 + Asp.2 (supported)**; Hyp.6's aggregation unit = section (row or same-color run). **Hyp.7 kept** — user: it "signals we may need to shift to cognition layer of analysis instead of kinematics that we are most probably doing now".
- **Phase-gated workflow adopted (user):** stop + notify at each phase end; user checks before next phase.

## Decisions (this session, user-dictated)

- Target: diagnose why MM/MPA/SC can't sort sections to match the color ranking; success criterion = **metrics reproduce the tier ranking (purple > orange > aqua)**; hypotheses **open to new metrics** beyond the triple.
- OIAH display: **.md + mermaid backbone** in `Temp/` (full text in file, concise summary in chat); link semantics supports (O→I→H) + constrains (Asp→*); link notation in the methodology doc (`O1+O2+O5 → I1+I3 → A2+A4 → H3`) is illustrative only — the planned map display governs.
- Orange tint rows 56–57 = accident (3 tiers only). Section definition confirmed (row, or run of consecutive same-color rows).
- Hyp.7 kept as the named competing branch — records the potential **cognition-layer** exit (vs kinematics) if geometry-based metrics fail.
- Session ended before Phase 4/5 by user choice.

## Resolved this session (Phase 4–5, user-dictated)

- **Phase 4 done:** reasoning map appended to `Temp/oiah_motor_adjustment.md` — mermaid backbone (Obs→Inf, 18 edges), per-hypothesis subgraphs (solid = supports, dashed = constrains), reverse link index R1/R2. Presented in chat; user reviewed and approved.
- **Placement decision (user):** the OIAH becomes the **main** hypothesis-clearing basis ("the pipeline generating them seems to be more reliable"); A/H/AA/HH **detached → reference-only** on [[motor-model-requirements]], kept for **gap-hunting** after the OIAH clears (any old item with no OIAH counterpart = possible gap). Merge-into-OIAH rejected (re-derivation cost + framework category violations).
- **R1–R9 (user question → Option C):** requirements are goals, not evidence — not pulled into the OIAH; they become the clearing loop's **acceptance layer**: coverage contract R↔Hyp + "Serves R#" on hypotheses; coverage check is a clearing-time gate. Coverage gaps found: **R4 chaos-of-change (old H9), R4 spread direction (old H8), R3 crossing depth/AR window (old H7)** → first gap-hunt candidates. Hyp.7 = meta branch (serves no R).
- **User verdicts:** coverage mapping ✓, gap candidates ✓, page name `motor-metrics-oiah` ✓, successor rule ✓ (new discoveries while clearing → Hyp.8+; gap-hunt leftovers → new reference notes; replaces 08-12 separate-lists rule for the active basis).
- **Wiki written:** [[motor-metrics-oiah]] created (full OIAH + map + status fields + coverage + gap-hunt); [[motor-model-requirements]] demoted (banner + headers + rules amendment); index updated (3 lines); log entries pending this handoff.

## Open decisions / next session

- **[main] Save ritual:** user reviews the wiki diff ([[motor-metrics-oiah]] new; [[motor-model-requirements]], index.md, log.md, this handoff changed) → commit on approval. Memory pointer `oiah-motor-diagnostic` update (artifact moved to wiki).
- **[main] After commit — clearing begins:** Hyp.1–7 validation loop against the 85-row tiered set (success criterion: reproduce P > O > A); coverage gate + gap-hunt at the end.
- [delegate] Deferred, not canceled (per 2026-08-12): official osu! reading-skill research (Q1/Q2 bucket), statistical "chaos of change" qualities search (H9 gate), physics-quantities search (Q4 — until H10/HH1 work).
- Parked: merge-question 3 forks; orchestration cache trial; user's in-game walk on the motor Excels; [[experiment-protocol]] keep/abandon — paused.

## Session end — next session (resume token: `resume:2026-08-13`)

- Recap-only per CLAUDE.md §6 until user authorizes execution.
- Next action: human review of the wiki diff → commit (save ritual); then the clearing loop on [[motor-metrics-oiah]].
- End-of-session lint (session-end policy) verdict recorded in log entry.

## Save state
Uncommitted (commit pending human approval per save ritual): wiki — [[motor-metrics-oiah]] (new), [[motor-model-requirements]] (demotion banner + headers + rules amendment), index.md (research + session lines), log.md (Phase 4/5 + lint entries), this handoff; Temp/ — `oiah_motor_adjustment.md` (working artifact, now with the Phase 4 map), `criteria_xlsx_full_dump.txt`, `tiered_rows_table.txt`, dump scripts (`dump_criteria_xlsx.py`, `dump_theme_colors.py`, `dump_tiers*.py`); memory — `oiah-motor-diagnostic` (pointer update pending commit).

## Sources
This session's discussion (user statements 2026-08-13), `Temp/OIAH definitions & pipeline.md`, `Prototyping/yoasobi_collab_extra_motor_patterns (success criteria here).xlsx` + extraction artifacts, `backend/src/analysis/reading/sequence_motor.rs` + `reading/mod.rs`, wiki [[sequence-motor]] / [[motor-model-requirements]] / [[reading-analysis]] / [[difficulty-philosophy]], `wiki/wiki/log.md` (entries 203, 206, 229), reader extraction reports; predecessor: [[2026-08-12-handoff]].
